{
  "name": "express-node-vue-boilerplate",
  "private": true,
  "scripts": {
    "dev": "concurrently --prefix-colors \"blue.bold,magenta.bold\" --prefix \"[{name}]\" --names \"server,client\" \"npm run dev --workspace=packages/back\" \"npm run dev --workspace=packages/front\""
  },
  "type": "module",
  "workspaces": [
    "packages/front",
    "packages/back"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
